/**
 * Компонент (Component) шаблону Декоратор.
 * Спільний інтерфейс для базового рядка та всіх декораторів.
 */
public interface Printable {
    /** Повертає поточний рядок. */
    String getString();
    /** Виводить рядок у консоль. */
    void print();
}
