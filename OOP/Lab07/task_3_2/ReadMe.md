# task_3_2 — Одинак Директор (доповнення 4.3.1)

Шаблон **Одинак** застосовано до класу `Director` з задачі 4.3.1:
у програмі може існувати лише один Директор автомобільного заводу.
- Приватний конструктор, `private static final Director instance`, `getInstance()`.
- Builder-компоненти (Engine/Wheel/Transmission/Car) з 4.3.1 без змін.

Запуск: `javac src/*.java && java -Dstdout.encoding=UTF-8 -cp src Main`
